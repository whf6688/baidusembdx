# 百度搜索信息流重构遗漏整改记录

整改日期：2026-09-15

整改范围：仅本地工作区和本地 Docker 容器

执行边界：未运行发布脚本；未连接或修改线上服务器；未执行真实百度写操作；未修改授权、Token、密钥或线上数据库

## 本次已完成

| 审计项 | 整改结果 | 验证证据 |
| --- | --- | --- |
| 可恢复基线 | 重新生成只含源码和部署定义的基线；排除 `.env`、Token、数据库、SSH 密钥和运行数据 | `data/refactor/baselines/2026-09-14T16-40-14-611Z`，254 个文件 |
| 隐藏旧账户表 | 已从 `App.tsx` 删除挂载逻辑、旧状态和旧列定义；同时移除只用于隐藏它的 CSS | Playwright 账户工作区三种宽度均断言不存在旧表 |
| 手动刷新 | 首次进入仍使用加载页；手动刷新进入后台更新状态并保留原数据；失败时不清空已加载数据 | 源码审查；前端构建通过 |
| 项目页状态 | 自动策略二级分区使用 `/{项目编码}?page=strategies&section={分区}`；刷新和复制链接可恢复；其他页面自动清除无关分区参数 | Playwright 项目路由用例通过 |
| 项目时区隔离 | 去除同源 `localStorage` 的全局时区写入；时区由当前项目设置加载到当前浏览器标签运行时 | 源码审查；前端组件及浏览器回归通过 |
| 工作台标题 | 物料中心和自动策略说明不再重复显示项目名称；项目名称保留在项目管理和跨项目确认场景 | 截图复核 |
| 无图标文字入口 | 顶部提醒保持可读的“提醒 99+”文字入口；装饰图标继续不占据视觉空间 | `account-workspace-1440.png` |
| 弹窗统一性 | 删除账户管家确认改为 Fluent Dialog；具备标准焦点管理、Esc 和关闭行为 | 前端组件测试通过 |
| 筛选层级 | 状态筛选改为弱蓝选中态，不与保存、发布、执行等主操作竞争 | 截图复核 |
| 内部节点文案 | 补齐计划、单元、关键词、人群、创意等常见节点映射；未知节点显示“未知节点” | 自动上线浏览器回归通过 |
| 短字段截断 | 管家类型等短字段取消省略；项目操作区调整为 1280 宽度可横向访问并保持主按钮白字对比 | `account-workspace-1440.png`、`project-management-1440.png` |

## 已验证的回归结果

| 检查 | 结果 |
| --- | --- |
| 后端测试 | 233 passed |
| 前端类型检查 | 通过 |
| 前端组件测试 | 3 个测试文件；8 passed |
| 前端生产构建 | 通过 |
| 浏览器回归 | 12 passed |

浏览器回归覆盖账户管家与账户列表的 1366、1440、1920 宽度；账户计划缓存展开；自动策略草稿、校验、试运行和发布入口；自动上线的四种账户选择和执行节点；项目入口、成员管理和项目刷新深链接；策略执行记录隔离；报表与物料分页

## 截图证据

- `docs/refactor/screenshots/account-workspace-1366.png`
- `docs/refactor/screenshots/account-workspace-1440.png`
- `docs/refactor/screenshots/account-workspace-1920.png`
- `docs/refactor/screenshots/account-list-expanded-1920.png`
- `docs/refactor/screenshots/auto-launch-records-1440.png`
- `docs/refactor/screenshots/project-management-1440.png`
- `docs/refactor/screenshots/strategy-project-refresh-1440.png`

## 尚未作为本次完成项的结构性工作

本次优先修复审计中会造成错误行为、状态串扰、可见缺陷和验收误判的问题；现有 `App.tsx` 与两份历史样式表仍然较大，尚未完全物理拆分为每页独立文件，也尚未把所有旧样式的字面量完全迁移到唯一 Token 文件

这不影响本次已验证的功能和本地使用；但后续若继续扩展页面，应安排独立的机械拆分批次，并以“无行为变化”为验收条件，不能与业务功能变更混合实施

## 本地运行状态

本机 `http://localhost:8280/` 已重建为本次源码版本；它仍是本地环境，不代表线上发布或线上数据变更

为符合本次本地验证范围，本机已将 `BAIDU_WRITES_ENABLED`、账户自动淘汰和创意自动重建均设为关闭；健康检查确认三项均为 `false`。恢复真实写入必须在单独验收后进行，不能作为本次整改的默认状态
