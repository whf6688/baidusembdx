# 本地迁移与回退说明

## 当前迁移

本地当前迁移头为 `20260914_0044`。本次重构涉及的增量迁移包括：

- 策略定义与版本；
- 试运行结果；
- 定时触发事实；
- 后台任务使用的策略版本引用。
- 管家统一返点与充值账户配置；
- 管家财务配置模式；
- 账户百度端状态快照。

默认策略在第一次读取或调度时从重构前常量初始化，不会改变已有候选和执行结果。

## 源码回退

基线目录：

`E:\百度搜索信息流\data\refactor\baselines\2026-09-13T14-29-07-372Z`

其中 `source.tar.gz` 已逐文件解压并通过 SHA-256 校验，记录在 `verification.json`。归档不含 `.env`、Token、数据库、SSH 密钥、`data` 或 `storage`。

回退前应先停止本地 web/api/worker/scheduler，并把当前源码另存为新归档。由于 `.git` 不是有效仓库，不使用 Git 覆盖工作区。将基线解压到单独目录核对后，再人工替换源码；不得覆盖 `.env`、`data` 和 `storage`。

项目路由的前端回退只影响页面入口；当前约定为系统首页 `/` 与项目工作台 `/{项目编码}?page={页面}`。回退前记录正在使用的项目链接；恢复旧版本后应从旧版本支持的入口重新进入项目，不需要改变项目、账户或授权数据

## 数据库回退

如仅回退本轮前端结构，不需要降级数据库。只有在确认没有新管家财务配置、账户状态快照、策略草稿或任务引用时，才可分步降级；降级前必须备份本地 PostgreSQL。

一般回退只需恢复前端和后端源码，新表可以安全保留；这样不会丢失策略审计记录。

## 线上状态

本次没有连接线上服务器，没有运行发布脚本，也没有执行线上数据库迁移。线上无需回退。
