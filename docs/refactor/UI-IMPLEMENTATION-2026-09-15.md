# 搜索信息流 UI 改造实施记录

## 本次完成内容

- 新增 `frontend/src/design-system.css`；作为搜索项目唯一的界面令牌层，统一画布、文字、边框、状态、按钮、表格、弹窗、分页和日期选择样式
- 页面使用蓝色 `#0969DA` 作为唯一交互主色；移除旧绿色视觉残留，成功、警告和危险状态只表达对应语义
- 应用壳统一为 216px 侧栏、48px 顶栏、24px 内容内边距；1280px 以下自动收起侧栏
- 统一页面标题、同行说明、工具栏、页签、表格行高、粘性分页器、弹窗和消息反馈
- 自动上线四个功能页签改为平级结构；移除账户选择、模板设置和执行时间的流程编号
- 自动搭建四种账户选择继续保留卡片与弹窗配置；卡片不再显示无意义编号
- 自动策略、项目刷新、参考模板和运行状态使用紧凑平级页签；移除策略工作区的强制整屏空白
- 表格正文统一 13px、400 字重；表头统一 12px、500 字重；金额、日期和比例使用等宽数字
- 账户管理、账户列表、物料、报表、成员和执行记录继续使用原接口与原数据口径
- Vite 开发代理支持 `VITE_API_PROXY_TARGET`；本地验证可指向现有容器 API，不改变生产接口地址

## 保持不变的功能

- 项目入口与 `/{projectCode}?page=...` 路由；多浏览器标签可以独立打开项目
- 账户管家页签、账户授权、删除管家后历史归档、余额与充值账户展示
- 账户列表的跨行选择、批量操作、指标概览和本地计划缓存展开
- 自动搭建优选与全量模式、四种账户选择、多日期多时间点、不限日期队列
- A–F 关键词分级、5000 关键词拆分、预检、限频、幂等、断点恢复和回读
- 自动策略草稿、校验、试运行、发布、版本历史和结果记录
- 后台统一计算的展现、点击、消费、加粉、CPM、CTR、加粉成本及现金口径

## 本地验证

- 源码恢复基线已生成并通过解压与哈希校验：`data/refactor/baselines/2026-09-14T17-19-42-949Z`
- 前端类型检查和生产构建通过
- Vitest：3 个文件、8 项测试通过
- Playwright：账户管理、自动上线、自动策略、项目管理、报表、账户列表和物料分页共 12 项测试通过
- 本地源码验证使用 `http://localhost:8281`；当容器 API 位于 8280 时，通过 `VITE_API_PROXY_TARGET=http://localhost:8280` 启动

## 回退方式

1. 停止本地前端开发服务
2. 将基线归档解压到隔离目录并比较 `manifest.json`
3. 恢复 `frontend/src/main.tsx`、`frontend/src/App.tsx`、`frontend/src/vite.config.ts` 和 `frontend/src` 下样式文件
4. 重新执行 `npm run build`、`npm test` 和 `npm run test:browser`

本次没有执行线上发布、线上数据库迁移或真实百度写入
